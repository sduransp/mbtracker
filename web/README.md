# Web Frontend

Minimal vanilla JS + CSS UI built with Vite.

- `index.html` bootstraps the app
- `main.js` contains the logic to call the API and render tables
- `style.css` contains base styles

Use `npm run dev` during development and `npm run build` for production.
