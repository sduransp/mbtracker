# Internationalization

The UI text is currently in English. To localize:
- Adjust strings in `web/main.js`
- Keep `web/index.html` lang attribute in sync
- Consider extracting strings into a simple `strings.js` for future multi-language support
